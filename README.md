# -IoT-TELEMETRY-PIPELINE-DASHBOARD
NET322: IoT Telemetry Pipeline — AsyncIO sensor simulator, Protobuf-based TCP server, REST API with content negotiation (JSON/XML/YAML), and WebSocket live dashboard for greenhouse monitoring.
# NET322 IoT Telemetry Pipeline

## Setup

Install dependencies:

```bash
pip install -r requirements.txt